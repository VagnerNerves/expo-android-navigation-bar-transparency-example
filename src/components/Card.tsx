import { Text, View } from "react-native";

export function Card() {
  return (
    <View
      style={{
        width: "100%",
        height: 100,
        backgroundColor: "#1e3a8a",
        borderRadius: 8,
      }}
    ></View>
  );
}
